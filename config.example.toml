data_dir = "data"  # base directory for credentials and bot data

[log]
level = "info" # debug, info, warn, error

[copilot]
model = "gpt-4o"

[openclaw]
host = "wss://your-openclaw-host.example.com"
token = "your-gateway-token"
